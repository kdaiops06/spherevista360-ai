import type { Metadata } from "next";
import { PredictionsCard } from "@/components/dashboard/PredictionsCard";
import { getPredictions } from "@/lib/fetch-live-data";
import { Brain, AlertTriangle } from "lucide-react";

export const metadata: Metadata = {
  title: "AI Market Predictions & Outlook",
  description:
    "AI-powered market predictions and outlook for stocks, crypto, currencies, and commodities. Data-driven analysis using machine learning.",
};

export default async function PredictionsPage() {
  const result = await getPredictions();

  return (
    <div className="container-main py-12">
      <div className="mb-10">
        <div className="flex items-center gap-3 mb-3">
          <Brain className="h-8 w-8 text-brand-600" />
          <h1 className="text-4xl font-bold text-gray-900">
            AI Market Predictions
          </h1>
        </div>
        <p className="text-lg text-gray-600">
          {result.isLive
            ? "Data-driven market outlook generated by AI analysis of economic indicators, market trends, and sentiment data"
            : "Sample AI predictions for demonstration — configure AI API keys to enable live analysis"}
        </p>
      </div>

      {/* Disclaimer */}
      <div className="mb-8 rounded-lg border border-yellow-200 bg-yellow-50 p-4">
        <div className="flex items-start gap-3">
          <AlertTriangle className="h-5 w-5 text-yellow-600 flex-shrink-0 mt-0.5" />
          <div>
            <p className="text-sm font-medium text-yellow-800">Disclaimer</p>
            <p className="text-sm text-yellow-700 mt-1">
              These predictions are {result.isLive ? "generated by AI" : "editorial analysis"} for educational and
              informational purposes only. They do not constitute financial
              advice. Always do your own research and consult a qualified
              financial advisor before making investment decisions.
            </p>
          </div>
        </div>
      </div>

      <div className="grid gap-6 lg:grid-cols-2">
        <PredictionsCard predictions={result.data.slice(0, 3)} isLive={result.isLive} source={result.source} lastUpdated={result.lastUpdated} />
        <PredictionsCard predictions={result.data.slice(3)} isLive={result.isLive} source={result.source} lastUpdated={result.lastUpdated} />
      </div>

      <div className="mt-8 card">
        <h2 className="text-xl font-bold text-gray-900 mb-4">
          How Our AI Predictions Work
        </h2>
        <div className="prose prose-gray max-w-none">
          <p>
            Our AI prediction system analyzes multiple data sources to generate
            market outlook:
          </p>
          <ul>
            <li>Real-time market data from Alpha Vantage</li>
            <li>Economic indicators from FRED (Federal Reserve)</li>
            <li>Currency exchange rate trends</li>
            <li>News sentiment analysis</li>
            <li>Historical pattern recognition</li>
          </ul>
          <p>
            Predictions are refreshed daily and include confidence scores to
            help you assess the strength of each outlook.
          </p>
        </div>
      </div>
    </div>
  );
}
